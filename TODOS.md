# TODOS

Generated by /plan-eng-review on 2026-04-19
Updated by /plan-design-review on 2026-04-19
Branch: main

## Blocking (must ship before first use)

- [ ] **Create `convex/auth.config.ts`** — Clerk JWT config so Convex trusts auth tokens. Without this, `ctx.auth.getUserIdentity()` returns null on every call. See Convex guidelines.
- [ ] **Fix `src/integrations/convex/provider.tsx`** — Replace `ConvexProvider` with `ConvexProviderWithAuth` from `convex/react-clerk`. Without this, auth tokens are never forwarded to Convex even if `auth.config.ts` exists.
- [ ] **Rewrite `convex/schema.ts`** — Replace demo `products`/`todos` tables with `portfolios`, `transactions`, `priceCache` tables per design doc. Update `priceCache` composite key to `ticker:currency` string.
- [ ] **Create `convex/mutations.ts`**: `createPortfolio` (with race-condition guard), `addTransaction` (with sell validation, auth guard, currency uppercase normalization, fxRate > 0 check), `deleteTransaction` (with net-qty simulation, auth guard)
- [ ] **Create `convex/actions.ts`**: `getHoldings(portfolioId)` — aggregates transactions, fetches prices + FX, returns typed array. Inline helpers: `fetchEquityPrice`, `fetchCryptoPrice`, `fetchFxRate`. Shared `getCachedOrFetch` pattern for cache read/write.
- [ ] **Use `identity.tokenIdentifier` everywhere** — Not `identity.subject`. Apply in all mutations and actions.
- [ ] **Build dashboard route** (`src/routes/dashboard.tsx`) — Holdings table (columns: TICKER | VALUE | P&L% | P&L | QTY | AVG COST), total portfolio value header, P&L per row. Two-phase load: render stale cache immediately with ⚠ indicator, fetch fresh prices in background, update rows in-place. Skeleton rows + "Fetching prices... N/M complete" progress indicator for cold load (no cache). Warm empty state (chart icon + CTA) for zero-transaction users.
- [ ] **Build Transactions page** (`src/routes/transactions.tsx`) — Full raw transaction log in reverse-chronological order. Columns: date | ticker | type | qty | price | currency | FX rate. Inline delete confirmation per row (row transforms to "Delete this transaction? [Confirm] [Cancel]"). Error state inline: "Cannot delete — a sell depends on this."
- [ ] **Build Add Transaction modal** — Wire FX auto-fetch from Frankfurter on date change (debounced). Hide FX field when `nativeCurrency === homeCurrency`. Toast on success ("Transaction added", 2s auto-dismiss). Note: "Include any fees in the price per unit."
- [ ] **First-time home currency prompt** — Modal on first sign-in, auto-creates default portfolio, never shows again. Copy: "Choose your home currency — your returns will be calculated in this currency. You can change it in Settings." EUR / USD / GBP / Other (ISO 4217 text input).

## Pre-launch (before sharing with others)

- [ ] **Math unit tests** — `avgCostBasis`, `unrealizedPnl`, `unrealizedPnlPct` covering buy-only, buy+sell, and multi-currency cases.
- [ ] **Alpha Vantage rate limit** — 5 req/min means cold load with 20 tickers takes ~4 min. Fix before multi-user launch (batch endpoint or provider swap).

## Dashboard UI

- [ ] Add disclaimer for sold positions: "Sold positions reduce holdings but cash proceeds are not tracked. Total value reflects current market value of open positions only."
- [ ] Stale price indicator: amber ⚠ SVG icon (not emoji) next to price with tooltip "Price may be up to 15 min old". On total value header: ⚠ stale badge when any price is stale.
- [ ] Add "Include any fees in the price per unit" note in Add Transaction form.

## Design System

- [ ] **Install shadcn/ui** — Run `bunx shadcn@latest init` and choose "Dark" theme. Tailwind v4 is already installed. Initialize with CSS variables. Add components needed for v1: `bunx shadcn@latest add table button input select dialog sheet skeleton badge toast tooltip`. This creates `src/components/ui/` and `components.json`.
- [ ] **Design tokens via shadcn CSS vars** — After shadcn init, override the generated CSS variables in `src/styles.css` to match the dark palette: `--background: 222 47% 7%` (≈`#0d0d14`), `--card: 222 40% 10%` (≈`#151520`), `--border: 222 30% 14%` (≈`#1e1e2e`), `--primary: 239 84% 67%` (≈`#6366f1`), `--muted-foreground: 220 9% 46%` (≈`#6b7280`). Also add custom tokens for positive/negative/warning using Tailwind CSS vars (these are not in shadcn's default set).
- [ ] **Geist font** — Install `geist` package (`bun add geist`). Import in `src/styles.css`. Set `font-variant-numeric: tabular-nums` via a Tailwind utility class `tabular-nums` on all price/percentage cells.
- [ ] **Mobile layout** — On ≤ 768px: sidebar collapses. Use shadcn `Sheet` component for the slide-over drawer. "Add Transaction" button in top-right of mobile header. Table on tablet (≤ 1024px): use Tailwind responsive classes to hide QTY, AVG COST, P&L absolute columns.
- [ ] **A11y baseline** — ARIA labels on all icon-only sidebar buttons. Keyboard nav: Tab through table rows, Enter to expand, Esc to close modals. shadcn Dialog and Sheet handle focus-trap and Esc key automatically.

### shadcn component map

| UI element | shadcn component |
|------------|-----------------|
| Holdings table | `Table`, `TableHeader`, `TableRow`, `TableCell` |
| Skeleton loading rows | `Skeleton` |
| Add Transaction modal | `Dialog`, `DialogContent`, `DialogHeader` |
| Home currency modal | `Dialog` |
| Mobile nav drawer | `Sheet`, `SheetContent` |
| All buttons | `Button` (variant: default, ghost, destructive) |
| Form inputs (ticker, qty, price) | `Input` |
| Selects (asset type, currency, buy/sell) | `Select`, `SelectTrigger`, `SelectContent`, `SelectItem` |
| Date picker | `Input` type="date" (native for v1; upgrade to `Popover` + calendar in v2) |
| Success toast | `Sonner` (shadcn's toast, via `bunx shadcn@latest add sonner`) |
| Stale price indicator | `Badge` (variant: outline, amber color override) + `Tooltip` |
| Icons (sidebar, delete, warning) | `lucide-react` (already installed) |

## V2 / Deferred

- [ ] Cash positions
- [ ] FIFO/LIFO tax-precise cost basis
- [ ] Broker API / CSV import
- [ ] Multi-portfolio per user
- [ ] Chronological net-quantity replay for backdated entries
- [ ] Exchange suffix support (ASML.AS vs ASML)
- [ ] Instrument identity beyond free-text ticker
- [ ] Fee/commission tracking field
