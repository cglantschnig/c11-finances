# TODOS

Generated by /plan-eng-review on 2026-04-19
Branch: main

## Blocking (must ship before first use)

- [ ] **Create `convex/auth.config.ts`** — Clerk JWT config so Convex trusts auth tokens. Without this, `ctx.auth.getUserIdentity()` returns null on every call. See Convex guidelines.
- [ ] **Fix `src/integrations/convex/provider.tsx`** — Replace `ConvexProvider` with `ConvexProviderWithAuth` from `convex/react-clerk`. Without this, auth tokens are never forwarded to Convex even if `auth.config.ts` exists.
- [ ] **Rewrite `convex/schema.ts`** — Replace demo `products`/`todos` tables with `portfolios`, `transactions`, `priceCache` tables per design doc. Update `priceCache` composite key to `ticker:currency` string.
- [ ] **Create `convex/mutations.ts`**: `createPortfolio` (with race-condition guard), `addTransaction` (with sell validation, auth guard, currency uppercase normalization, fxRate > 0 check), `deleteTransaction` (with net-qty simulation, auth guard)
- [ ] **Create `convex/actions.ts`**: `getHoldings(portfolioId)` — aggregates transactions, fetches prices + FX, returns typed array. Inline helpers: `fetchEquityPrice`, `fetchCryptoPrice`, `fetchFxRate`. Shared `getCachedOrFetch` pattern for cache read/write.
- [ ] **Use `identity.tokenIdentifier` everywhere** — Not `identity.subject`. Apply in all mutations and actions.
- [ ] **Build dashboard route** (`src/routes/dashboard.tsx`) — Holdings table, total value, P&L per row, skeleton while `getHoldings` loads.
- [ ] **Build Add Transaction modal** — Wire FX auto-fetch from Frankfurter on date change (debounced). Hide FX field when `nativeCurrency === homeCurrency`.
- [ ] **First-time home currency prompt** — Modal on first sign-in, auto-creates default portfolio, never shows again.

## Pre-launch (before sharing with others)

- [ ] **Math unit tests** — `avgCostBasis`, `unrealizedPnl`, `unrealizedPnlPct` covering buy-only, buy+sell, and multi-currency cases.
- [ ] **Alpha Vantage rate limit** — 5 req/min means cold load with 20 tickers takes ~4 min. Fix before multi-user launch (batch endpoint or provider swap).

## Dashboard UI

- [ ] Add disclaimer for sold positions: "Sold positions reduce holdings but cash proceeds are not tracked. Total value reflects current market value of open positions only."
- [ ] Show "price may be stale" label when serving from cache.
- [ ] Add "Include any fees in the price per unit" note in Add Transaction form.

## V2 / Deferred

- [ ] Cash positions
- [ ] FIFO/LIFO tax-precise cost basis
- [ ] Broker API / CSV import
- [ ] Multi-portfolio per user
- [ ] Chronological net-quantity replay for backdated entries
- [ ] Exchange suffix support (ASML.AS vs ASML)
- [ ] Instrument identity beyond free-text ticker
- [ ] Fee/commission tracking field
